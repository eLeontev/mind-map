import {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAuthorized,
    signOff,
} from './map-menu.service';

export let services = {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAuthorized,
    signOff,
};
