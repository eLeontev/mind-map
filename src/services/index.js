import {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAuthorized,
} from './map-menu.service';

export let services = {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAuthorized,
};
