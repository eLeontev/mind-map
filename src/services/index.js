import {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAutorized,
} from './map-menu.service';

export let services = {
    getMaps,
    createMap,
    getMapByID,
    saveMapByID,
    isUserAutorized,
};
